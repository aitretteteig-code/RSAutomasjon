# Test Library

This folder contains reusable test definitions and test sets.

Primary sets:
- `helsesjekk/test-set.md`
- `regresjon/test-set.md`

Individual test definitions live in each set's `tests/` folder.

Use templates from `test-artifacts/templates/` when creating new tests.
